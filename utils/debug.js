let contenedorInfo = ''
    for(let i = 0; i < 30 ; i++) {
        if(i <= 10) {
            contenedorInfo += `<div>entra al div inicial ${i}
                                    </div>`
        }
    }

    console.log(contenedorInfo)